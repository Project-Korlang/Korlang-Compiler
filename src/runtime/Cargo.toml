[package]
name = "korlang-runtime"
version = "0.1.0"
edition = "2021"

[lib]
name = "korlang_rt"
crate-type = ["staticlib", "rlib"]

[dependencies]
libc = "0.2"
serde = { version = "1", features = ["derive"] }
serde_json = "1"

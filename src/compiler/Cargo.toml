[package]
name = "korlang-compiler"
version = "0.1.0"
edition = "2021"

[dependencies]
thiserror = "1.0"
inkwell = { version = "0.8.0", features = ["llvm18-1"], optional = true }

[features]
default = ["llvm"]
llvm = ["dep:inkwell"]

#[dev-dependencies]
#criterion = "0.5"

#[[bench]]
#name = "compiler_bench"
#harness = false
